# Create a .env.local file with this content:
DATABASE_URL="postgresql://postgres:password@localhost:5432/gym_management"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-super-secret-nextauth-key-here-change-this"

# Optional: Generate a better secret with:
# openssl rand -base64 32
